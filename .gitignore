# macOS
.DS_Store

# Pipeline build output (regenerated by `python -m pipeline.build`).
# webapp/data.js is NOT ignored -- it is committed on purpose so the site
# works without running Python.
out/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
